# רשימת תיוג — אדמין מערכת (Logi8173)

## 0. דרישות מוקדמות

- [ ] חשבון Google גדודי (לא אישי) — החשבון הזה יהיה הבעלים של כל המבנה ב-Drive ו-Sheets
- [ ] גישה ל-Google Cloud Console עם אותו חשבון
- [ ] גישה ל-Google Apps Script Editor
- [ ] גישה ל-Vercel Dashboard
- [ ] קובץ Excel/Sheets עם מלאי גדודי (קטלוגי, שמות, כמויות)
- [ ] קובץ Excel/Sheets עם רשימת חיילים (מ"א, שם, פלוגה, מחלקה)

---

## 1. הגדרת Google OAuth

### מה לעשות
1. היכנס ל-[Google Cloud Console](https://console.cloud.google.com)
2. צור פרויקט חדש (או השתמש בקיים)
3. הפעל את **Google Sheets API** ו-**Google Drive API**
4. לך ל-**APIs & Services → Credentials**
5. צור **OAuth 2.0 Client ID** מסוג **Web Application**
6. הוסף ל-Authorized JavaScript origins:
   - `https://your-app.vercel.app` (הדומיין בפרודקשן)
   - `http://localhost:5173` (לפיתוח מקומי)
7. הוסף ל-Authorized redirect URIs:
   - `https://your-app.vercel.app`
   - `http://localhost:5173`
8. שמור את ה-**Client ID** — תצטרך אותו בשלבים הבאים

### תוצאה צפויה
- יש לך Client ID בפורמט `XXXXX.apps.googleusercontent.com`

### שגיאות נפוצות
- **"origin mismatch"** — הדומיין לא נוסף ל-origins. ודא שאין סלאש בסוף
- **"redirect_uri_mismatch"** — אותו דבר עם redirect URIs

---

## 2. פריסת Apps Script

### מה לעשות
1. פתח את [Apps Script Editor](https://script.google.com)
2. צור פרויקט חדש והעתק את כל הקבצים מתיקיית `apps-script/`
3. הגדר **Script Properties** (הגדרות → מאפייני סקריפט):
   - `SETUP_ADMIN_EMAIL` = כתובת המייל של החשבון הגדודי
   - `WEB_CLIENT_ID` = ה-Client ID שקיבלת בשלב 1
   - `BREAK_GLASS_ADMIN_EMAIL` = `nivdam@gmail.com` (גישת חירום מרחוק)
4. לך ל-**Deploy → New deployment**
5. בחר **Web app**
6. הגדרות:
   - Execute as: **User accessing the web app**
   - Who has access: **Anyone**
7. לחץ **Deploy** ואשר את כל ההרשאות (Drive, Sheets, External Requests)
8. העתק את כתובת ה-`/exec` URL

### תוצאה צפויה
- יש לך URL בפורמט `https://script.google.com/macros/s/.../exec`
- ההרשאות אושרו בהצלחה

### שגיאות נפוצות
- **"Authorization required"** — לא אישרת את כל ה-scopes. לחץ Deploy מחדש ואשר
- **"Script function not found"** — ודא שהקובץ `Main.gs` קיים עם `doPost` ו-`doGet`

---

## 3. הגדרת Vercel

### מה לעשות
1. היכנס ל-[Vercel Dashboard](https://vercel.com/dashboard)
2. לך ל-**Settings → Environment Variables** של הפרויקט
3. הגדר:
   - `VITE_GOOGLE_CLIENT_ID` = ה-Client ID מ-שלב 1
   - `APPS_SCRIPT_URL` = ה-`/exec` URL מ-שלב 2
4. בצע Deploy מחדש (או push חדש)

### תוצאה צפויה
- האפליקציה עולה ב-Vercel בלי שגיאות build
- דף הלוגין נטען

### שגיאות נפוצות
- **מסך לבן** — בדוק ב-Vercel Logs שגיאות build. ייתכן שחסר משתנה סביבה
- **"CORS error"** — ודא שהדומיין נוסף ל-Authorized origins ב-Google Cloud Console

---

## 4. הרצת Setup (פעם אחת)

### מה לעשות
1. פתח את האפליקציה בדפדפן עם **החשבון הגדודי**
2. לחץ על **התחבר עם Google**
3. המערכת תזהה שזה הפעם הראשונה ותציג מסך Setup
4. לחץ על **הפעל הקמת מערכת** (או כפתור דומה)
5. המתן לסיום — המערכת תיצור:
   - תיקיית Drive ראשית
   - גליונות: מלאי ראשי, מפעילים, חיילים, פלוגות, רישום פעילויות
   - תיקיות פעילויות

### תוצאה צפויה
- מוצגת הודעת הצלחה
- מועבר לדשבורד הראשי
- ב-Google Drive נוצרה תיקיית "Logi8173" עם גליונות ריקים

### שגיאות נפוצות
- **"SETUP_ALREADY_DONE"** — ה-Setup כבר רץ. אם צריך לאפס — מחק את Script Properties שנוצרו (ROOT_FOLDER_ID וכו')
- **"MISSING_TOKEN"** — בעיית auth. ודא שה-Client ID זהה בין Vercel ל-Apps Script

### עצור ובדוק
- [ ] הדשבורד נטען ללא שגיאות
- [ ] ב-Drive קיימת תיקייה עם גליונות

---

## 5. הוספת מפעילים

### מה לעשות
1. לך ל-**הגדרות** (`/settings`)
2. בחר בלשונית **מפעילים**
3. לחץ **הוסף מפעיל**
4. מלא:
   - כתובת מייל (חשבון Google של המפעיל)
   - שם
   - תפקיד: `admin` / `warehouse_operator` / `commander` / `viewer`
5. שמור

### תוצאה צפויה
- המפעיל מופיע ברשימה
- המפעיל יכול להתחבר עם חשבון ה-Google שלו

### שגיאות נפוצות
- **מפעיל לא מצליח להתחבר** — ודא שכתובת המייל זהה בדיוק (כולל אותיות גדולות/קטנות)
- **"FORBIDDEN"** — רק admin יכול להוסיף מפעילים

### עצור ובדוק
- [ ] בקש ממפעיל אחד להתחבר ולוודא שהוא רואה את הדשבורד
- [ ] ודא שמפעיל עם תפקיד `viewer` לא יכול לבצע הנפקות

---

## 6. הוספת פלוגות

### מה לעשות
1. לך ל-**הגדרות** (`/settings`)
2. בחר בלשונית **פלוגות**
3. לחץ **הוסף פלוגה**
4. מלא שם פלוגה
5. חזור על כל הפלוגות בגדוד

### תוצאה צפויה
- כל הפלוגות מופיעות ברשימה
- הפלוגות זמינות בבחירה בעת ייבוא חיילים

### שגיאות נפוצות
- **שם כפול** — המערכת אמורה להתריע. שנה את השם

---

## 7. ייבוא מלאי

### מה לעשות
1. לך ל-**מלאי** (`/inventory`)
2. לחץ **ייבוא**
3. פתח את קובץ ה-Excel/Sheets עם המלאי הגדודי
4. סמן את הנתונים (בלי כותרות) והעתק (Ctrl+C)
5. הדבק בתיבת הטקסט
6. בדוק את התצוגה המקדימה — כל שורה צריכה להיות מזוהה
7. תקן שגיאות אם יש (שורות אדומות)
8. לחץ **אשר ייבוא**
9. המתן לסיום — השורות נכתבות אחת-אחת

### תוצאה צפויה
- כל הפריטים מופיעים ברשימת המלאי
- סה"כ פריטים תואם למקור

### שגיאות נפוצות
- **שורות נכשלות** — בדוק שהפורמט תואם (TSV — מופרד טאבים). אם העתקת מ-Excel, זה אמור לעבוד אוטומטית
- **ייבוא איטי** — זה נורמלי. כל שורה נכתבת בנפרד. אם יש יותר מ-200 שורות, תהיה סבלני
- **"FORBIDDEN"** — רק admin או warehouse_operator יכולים לייבא

### עצור ובדוק
- [ ] רשימת המלאי מציגה את כל הפריטים
- [ ] החיפוש והסינון עובדים
- [ ] הכמויות תואמות למקור

---

## 8. ייבוא חיילים

### מה לעשות
1. לך ל-**חיילים** (`/soldiers`)
2. לחץ **ייבוא**
3. פתח את קובץ ה-Excel/Sheets עם רשימת החיילים
4. סמן והעתק (Ctrl+C)
5. הדבק בתיבת הטקסט
6. בדוק תצוגה מקדימה
7. לחץ **אשר ייבוא**

### תוצאה צפויה
- כל החיילים מופיעים ברשימה
- כל חייל משויך לפלוגה נכונה

### שגיאות נפוצות
- **פלוגה לא מזוהה** — ודא שהפלוגות הוגדרו בשלב 6 לפני הייבוא
- **מ"א כפול** — המערכת תדלג או תעדכן (upsert)

### עצור ובדוק
- [ ] רשימת החיילים מציגה את כל החיילים
- [ ] סינון לפי פלוגה עובד
- [ ] אין חיילים "יתומים" ללא פלוגה

---

## 9. פתיחת פעילות

### מה לעשות
1. לך ל-**פעילויות** (`/activities`)
2. לחץ **פתח פעילות חדשה**
3. מלא:
   - שם פעילות
   - תיאור (אופציונלי)
4. בחר פריטי מלאי לצילום מצב (snapshot) — רק פריטים רלוונטיים לפעילות הזו
5. לחץ **פתח פעילות**

### תוצאה צפויה
- הפעילות מופיעה ברשימה עם סטטוס "פתוחה"
- ב-Google Drive נוצרה תיקייה ייעודית לפעילות
- גליונות: inventory-snapshot, transactions, incidents, audit-log

### שגיאות נפוצות
- **"No items selected"** — חובה לבחור לפחות פריט אחד
- **יצירה איטית** — יצירת תיקייה וגליונות ב-Drive לוקחת כמה שניות, זה נורמלי

### עצור ובדוק
- [ ] הפעילות מופיעה ברשימת הפעילויות
- [ ] לחיצה על הפעילות מציגה את פריטי ה-Snapshot
- [ ] ב-Drive יש תיקייה חדשה עם 4 גליונות

---

## 10. אימות תקינות מלא

לפני מסירה ליחידה, בצע את הבדיקות הבאות:

- [ ] מפעיל שני מצליח להתחבר
- [ ] מפעיל שני רואה את הפעילות הפתוחה
- [ ] הנפקת ציוד עובדת (ראה רשימת תיוג מפעיל)
- [ ] החזרת ציוד עובדת
- [ ] הרענון של כל דף לא שובר את האפליקציה
- [ ] האפליקציה עובדת בנייד
- [ ] שיתוף קישור לטופס עובד

---

## פקודות חירום

### מפעיל נעול בחוץ
1. התחבר עם חשבון `nivdam@gmail.com` (break-glass admin)
2. לך להגדרות → מפעילים
3. הוסף/ערוך את המפעיל

### המערכת לא מגיבה
1. בדוק ב-Vercel Dashboard שה-deployment תקין
2. בדוק ב-Apps Script שה-deployment פעיל
3. ודא שה-Script Properties לא נמחקו

### נתונים לא נכונים
- **אל תמחק שורות ידנית מהגליונות** — זה ישבור את חישובי המלאי
- צור פעילות חדשה עם הנתונים הנכונים
